package main

import (
	"fmt"
	"testing"
)

func TestAsyncHashUserEmail(t *testing.T) {
	// assert that the channel is closed
	t.Run("Channel is closed", func(t *testing.T) {
		fmt.Println("running nothing...")
	})

}
